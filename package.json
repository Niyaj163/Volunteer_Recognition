{
  "name": "volunteer-recognition-root",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run install-frontend && npm run build-frontend",
    "install-frontend": "npm install --prefix frontend",
    "build-frontend": "npm run build --prefix frontend"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "firebase-admin": "^14.1.0"
  }
}
